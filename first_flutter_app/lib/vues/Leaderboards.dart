import 'package:flutter/material.dart';

class LeaderB extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is LeaderBoard Component'),
    );
  }
}
